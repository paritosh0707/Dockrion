[tool.uv.workspace]
members = [
  "packages/common-py",
  "packages/schema",
  "packages/adapters",
  "packages/policy-engine",
  "packages/telemetry",
  "packages/sdk-python",
  "packages/cli"
]

[tool.uv.sources]
agentdock-common = { workspace = true }
agentdock-schema = { workspace = true }
agentdock-adapters = { workspace = true }
agentdock-policy = { workspace = true }
agentdock-telemetry = { workspace = true }
agentdock-sdk = { workspace = true }
agentdock-cli = { workspace = true }

# Optional shared tool config
[tool.ruff]
line-length = 100
target-version = "py312"