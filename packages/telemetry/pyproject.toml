[project]
name = "agentdock-telemetry"
version = "0.1.0"
description = "Telemetry helpers"
requires-python = ">=3.12"
dependencies = ["prometheus-client>=0.20"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
