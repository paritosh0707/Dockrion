[project]
name = "agentdock-common"
version = "0.1.0"
description = "Common utilities for AgentDock"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.5"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
