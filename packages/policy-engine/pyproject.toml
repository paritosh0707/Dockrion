[project]
name = "agentdock-policy"
version = "0.1.0"
description = "Policy engine (redaction, tool gating)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
